import "lang/assert.h7"

def app|main()
  assert("abc".slice*(0u, 3u)[0u] == \a)

  let t = "hello world".slice*(3u, 7u)
  assert(t.num-items == 4u)
  assert(t[0u] == \l)
  assert(t[1u] == \o)
  assert(t[2u] == \space)
  assert(t[3u] == \w)

  0
