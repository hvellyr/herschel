module theatre ("eyestep/theatre 1.0")
  ~ The @code{theatre} module contains the public theatre API. ~

-- interface

def f0(x) : Int
  ~ some blabla ~
  x * 2

def f1(x) : Int ...
  ~ some blabla ~

def const x
    ~ xxx ~
    = 5

def const x : Int
    ~ xxx ~
    = 5


def fluid *stdout* : Port
    ~ standard out ~
    = TTYPort(0)

def config OS
    ~ define the operating system to build for ~
    = "mac-os-x"

-- def class
-- def type
-- def alias
-- def slot

def enum FileMode
{
  read
    ~ Open files for reading. ~
    = 1 << 0,
  write
    ~ Open files for writing. ~
    = 1 << 1,
  append
    ~ Open files for writing and put cursor to end of file. ~
    = 1 << 2,
  create
    ~ The file should be created when opening. ~
    = 1 << 3,
  exclusive
    ~ Open file in exclusive mode.

      This is normally combined with @code{create}.  The file must not
      exist when created. ~
    = 1 << 4
}

-- def enum Color : Keyword
-- {
--   red    = #red,
--   orange = #orange,
--   yellow = #yellow,
--   green  = #green
-- }

--   x ... =
-- def measure
-- def unit
-- def char
-- def macro
