module japan

export public(City)

def class City(_name, _country)
{
  def slot name = _name
  def slot country = _country
}


def class Person(_name, _job)
{
  def slot name = _name
  def slot job = _job
}
