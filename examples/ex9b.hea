def generic add-all(self @ Collection<'T>, other @ Collection<'T>) : Collection<'T> ...

def generic insert(self @ Sequence<'T>,
                value @ 'T,
                before-index : Int) : Sequence<'T> ...

def generic forall(proc : (Function(a : Vector<Real>, b : Int) : Bool)) : Bool ...

def generic add-all(x : Vector<a, b>) : Bool ...

