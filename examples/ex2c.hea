namespace abc("de.sijo.abc")

def macro cvar
{
  { def cvar ?a:name = ?init:expr } -> { def ?a = ?init * 2 }
  { let cvar ?a:name = ?init:expr } -> { let ?a = ?init * 2 }
}


namespace xyz("de.sijo.xyz")

def f()
  let cvar y = 11
  y

def cvar x = 127

