def macro wait
{
  { on wait(?n:name, ?x:name) ?b:expr } -> { { let ?n = 7
                                               let ?x = true
                                               ?b } }
}

def h()
  let xyz(i) i ** 2

  on wait(a, b) {
    xyz(a)
  }
